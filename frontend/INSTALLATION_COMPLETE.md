# Installation notes

Install **Node.js dependencies** for the React app:

```bash
cd frontend
npm install
npm start
```

There is **no Python backend** in this repository anymore. Analysis and PDF features require your own API if you add one later.
